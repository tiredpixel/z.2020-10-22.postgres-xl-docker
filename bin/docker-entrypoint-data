#!/bin/sh -eu

test -d "$PGDATA" || docker-init-data

# replace subnet in case it has changed
docker-reinit-data
# replace setup catalogue in case it has changed
sleep 30 && init-eg-entrypoint &

exec "$@"
